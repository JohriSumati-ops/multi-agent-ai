"""
knowledge_graph/ — RESERVED FOR PHASE 4

WHY THIS FOLDER EXISTS (already, even though empty of logic)
---------------------------------------------------------------
This package will hold concept extraction, graph construction, and graph
traversal/query logic (see Architecture Section 3.2 and Section 6.2's
Knowledge Graph Agent). `models/learning_profile.py` already anticipates
this: `weak_topics`/`strong_topics` are plain string arrays today and will
migrate to proper foreign keys against this package's future node table
once it exists.

NO KNOWLEDGE GRAPH LOGIC EXISTS YET. Do not import from this package.
"""
