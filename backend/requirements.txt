# --- Web framework ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30

# --- Settings & validation ---
pydantic>=2.7,<3.0
pydantic-settings>=2.3
email-validator>=2.1          # required for pydantic's EmailStr

# --- Database ---
sqlalchemy>=2.0,<3.0
psycopg2-binary>=2.9
alembic>=1.13                 # migrations — not yet wired to a schema, ready for Phase 2

# --- Auth skeleton ---
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
bcrypt<4.1                    # passlib's bcrypt backend is incompatible with bcrypt>=4.1 as of this writing

# --- Config ---
python-dotenv>=1.0

# --- Testing ---
pytest>=8.0
httpx>=0.27                   # required by FastAPI's TestClient

# --- Phase 2: document processing ---
pypdf>=5.0                    # PDF text extraction
python-docx>=1.1              # DOCX text extraction
pysbd>=0.3                    # rule-based sentence boundary detection (classical NLP, no data download)
langdetect>=1.0               # statistical n-gram language detection (classical NLP, no data download)
python-multipart>=0.0.9       # required by FastAPI for multipart/form-data file uploads

# --- Phase 2: test-only (used to generate sample PDFs/DOCX in the test suite) ---
reportlab>=4.0

# --- Phase 3: semantic retrieval ---
sentence-transformers>=3.0    # local embedding inference (pulls in torch)
faiss-cpu>=1.8                # vector index (exact search, IndexFlatIP + IndexIDMap)
numpy>=1.26